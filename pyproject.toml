[project]
name = "systemteams-aws-inventory"
version = "0.1.0"
description = "CLI para listar metadata de SSM Parameter Store e Secrets Manager"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.34.0,<2",
    "tabulate>=0.9.0,<1",
]

[tool.uv]
package = false
